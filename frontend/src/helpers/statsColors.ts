// A brighter, more vivid green than the app's own (fairly dark) theme success color - just for "ok" across the EJS stats feature.
export const OK_GREEN = "#4caf50";

// The pie's ok-text sub-slices, from most to least emphatic - still all clearly brighter than the old theme green.
export const OK_GREEN_BY_TEXT: Record<string, string> = { OK: "#43a047", Ok: "#66bb6a", ok: "#a5d6a7" };
