# Base URL of the API this frontend talks to (see helpers/apiCall.js's apiSuffix).
# Local dev: http://localhost:4001 (or whatever PORT api/.env uses).
# Production (Vercel): set to the deployed API's URL.
VITE_HTTPS_PROXY=
